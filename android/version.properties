# Canonical Android app version (read by Gradle + CI).
# versionName = user-facing semver (Settings → About, GitHub release tag android-vX.Y.Z)
# versionCode = integer that MUST increase with every Play/sideload update
VERSION_NAME=1.0.0
VERSION_CODE=1
